<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>
hello
<%= @song.name %>
<a href="/artists/<%= @song.artist.slug%>">
<h1><%=@song.artist.name%></h1> </a>


<% @song.genres.each do |genre| %>
	<a href="/genres/<%=genre.slug%>">
	<%= genre.name %>
	<%end%>

<h2>test</h2>
	<form action="/songs/<%=@song.slug%>" method="post">
	<input type="submit" value="Edit">
	</form>